extract https://archive.org/download/stackexchange/mathematica.stackexchange.com.7z to ./xml

cd into ./scripts
set up the database by running python3 prepare_data.py
train the model by running bash train.sh